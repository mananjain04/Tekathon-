[pytest]
pythonpath = .
markers =
    real_embedding_model: opts out of the autouse fake embedding model fixture and loads the real all-MiniLM-L6-v2 model (requires internet on first run to download it; slow).
