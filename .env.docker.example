# ==============================================================================
# KAVACH Sovereign Document AI - Docker Environment Configuration
# Copy this file to `.env` in the root directory before running docker-compose
# ==============================================================================

# --- Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=KavachSecurePass2026!
POSTGRES_DB=sih_rag

# --- Security / JWT Auth ---
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=bc5364f96e20e08df4fcaacb7009ff8a960a98ef06fcdd605c3f083455a079f8

# --- LLM Provider (Ollama) ---
# For containerized Ollama (CPU):
OLLAMA_BASE_URL=http://ollama:11434

# For host machine Ollama (NVIDIA RTX GPU acceleration):
# OLLAMA_BASE_URL=http://host.docker.internal:11434

OLLAMA_MODEL=qwen2.5:latest
